# context-menu テストケース

> ## ⚠ この表の `[x]` は、**撤去した WinUI 3 版**のものです
>
> 2026-09-12 に UI 基盤を **素の Win32 ＋ Native AOT** へ載せ替え、旧実装を撤去しました。
> **ここの `[x]` / `[ ]` は旧実装での結果**で、現行実装の合否ではありません。
>
> **現行実装での状況は [e2e-coverage.md](../../win32-migration/e2e-coverage.md) を見てください。**
>
> **この違いは実際に不具合を隠しました。**`drag-drop` は旧実装では 16 件すべて
> `[ ]` / `[~]`（合成マウスを WinUI が受け付けず自動化できなかったため）ですが、
> 現行実装ではいちばん厚く確認した領域です。逆に `[x]` が並んでいても
> 現行実装では一度も触っていないものがありました——
> [BUG-034](../../../issues/tickets/BUG-034.md)（移植し忘れ 3 件）は、
> **この表を `[x]` のまま信じていたら見つかりませんでした。**


## ユニットテスト

テストプロジェクトは未整備（初期版は手動確認。親 dev-notes.md 参照）。

| ID | テスト名 | 入力 / 条件 | 期待結果 | ステータス |
|----|---------|------------|---------|-----------|
| U-01 | （未整備） | — | — | [ ] |

## インテグレーションテスト

| ID | テスト名 | 前提条件 | 手順 | 期待結果 | ステータス |
|----|---------|---------|------|---------|-----------|
| I-01 | （未整備） | — | — | — | [ ] |

## E2Eテスト（手動）

| ID | テスト名 | 前提条件 | 手順 | 期待結果 | ステータス |
|----|---------|---------|------|---------|-----------|
| E-01 | 項目メニュー表示 | 任意フォルダ表示中 | ファイルを右クリック | 項目が選択され、カーソル位置にエクスプローラー同等のメニューが出る | [x] |
| E-02 | 開く | E-01 | テキストファイルで「開く」等を実行 | 既定アプリで開く | [x] |
| E-03 | コピー→貼り付け | E-01 | ファイルで「コピー」→空白右クリック「貼り付け」 | 同フォルダに「- コピー」が作られる（表示は再移動で確認） | [x] |
| E-04 | 削除 | E-01 | 不要ファイルで「削除」 | シェルの確認/ごみ箱動作がそのまま行われる | [x] |
| E-05 | プロパティ | E-01 | 「プロパティ」を実行 | シェルのプロパティダイアログが開く | [x] |
| E-06 | 送る/プログラムから開く | E-01 | 「送る」「プログラムから開く」をポイント | サブメニューの中身が空でなく展開される（IContextMenu2/3 転送の確認） | [x] |
| E-07 | 背景メニュー | 任意フォルダ表示中 | 一覧の空白部分を右クリック | 「新規作成」「貼り付け」等を含む背景メニューが出る | [x] |
| E-08 | 新規作成 | E-07 | 「新規作成」→「フォルダー」 | フォルダが作られる（表示は再移動で確認） | [x] |
| E-09 | キャンセル | E-01 | メニュー外クリック / Esc | 何も実行されず閉じる | [x] |
| E-10 | フォルダ項目のメニュー | 任意フォルダ表示中 | サブフォルダを右クリック | フォルダ向けメニュー（開く/プロパティ等）が出て動作する | [x] |
| E-11 | AOT 安定性 | 正式(AOT)ビルド | E-01〜E-10 を AOT 版で実施 | クラッシュ・WER イベントなし | [x] |

> 検証: 2026-07-13（同上フィードバック / commit `bb099c7` / run `29194702899` / 正式 AOT ビルド）。10/11 合格。
> E-03 `[!]`: コピーは可能だが背景メニューに「貼り付け」が存在せず貼り付け不可 → [BUG-003](../../../issues/tickets/BUG-003.md)。
> E-11: 全メニュー操作でクラッシュ・WER・新規ダンプなし（AOT 安定性確認済み）。「名前の変更」は自動化では未確認。

> E-03 再検証: 2026-07-20（commit `9f31c9e`）。BUG-003（背景の貼り付け欠落）・BUG-005（同フォルダ自動リネーム）の修正により合格。同フォルダは「- コピー」自動生成、別フォルダ同名は標準の置換/スキップ/両方保持ダイアログ。

## ステータス凡例

- `[ ]` 未実施
- `[~]` 実施中
- `[x]` 合格
- `[!]` 不合格
