# drag-drop テストケース

> ## ⚠ この表の `[x]` は、**撤去した WinUI 3 版**のものです
>
> 2026-09-12 に UI 基盤を **素の Win32 ＋ Native AOT** へ載せ替え、旧実装を撤去しました。
> **ここの `[x]` / `[ ]` は旧実装での結果**で、現行実装の合否ではありません。
>
> **現行実装での状況は [e2e-coverage.md](../../win32-migration/e2e-coverage.md) を見てください。**
>
> **この違いは実際に不具合を隠しました。**`drag-drop` は旧実装では 16 件すべて
> `[ ]` / `[~]`（合成マウスを WinUI が受け付けず自動化できなかったため）ですが、
> 現行実装ではいちばん厚く確認した領域です。逆に `[x]` が並んでいても
> 現行実装では一度も触っていないものがありました——
> [BUG-034](../../../issues/tickets/BUG-034.md)（移植し忘れ 3 件）は、
> **この表を `[x]` のまま信じていたら見つかりませんでした。**


## ユニットテスト

テストプロジェクトは未整備（初期版は手動確認。親 dev-notes.md 参照）。

| ID | テスト名 | 入力 / 条件 | 期待結果 | ステータス |
|----|---------|------------|---------|-----------|
| U-01 | （未整備） | — | — | [ ] |

## インテグレーションテスト

| ID | テスト名 | 前提条件 | 手順 | 期待結果 | ステータス |
|----|---------|---------|------|---------|-----------|
| I-01 | （未整備） | — | — | — | [ ] |

## E2Eテスト（手動）

| ID | テスト名 | 前提条件 | 手順 | 期待結果 | ステータス |
|----|---------|---------|------|---------|-----------|
| E-01 | ペイン間移動 | 2 ペイン表示 | 左のファイルを右の一覧へドラッグ | 移動される（元から消え、**その場で表示にも反映される**。BUG-008） | [~] |
| E-02 | Ctrl でコピー | 同上 | Ctrl を押しながらドロップ | コピーされる（元が残る） | [~] |
| E-03 | 複数項目 | 複数選択してドラッグ | 別ペインへドロップ | 全項目が移動される | [~] |
| E-04 | フォルダの D&D | フォルダをドラッグ | 別ペインへドロップ | フォルダごと移動される | [~] |
| E-05 | 本アプリ→エクスプローラー | 両方表示 | 本アプリからエクスプローラーへドロップ | エクスプローラー側に移動/コピーされる | [~] |
| E-06 | エクスプローラー→本アプリ | 同上 | エクスプローラーから本アプリへドロップ | 表示中フォルダへ移動/コピーされる | [~] |
| E-07 | 同フォルダへのドロップ | 同一ペイン内 | 同じ一覧へドロップ | 何も起きない | [~] |
| E-08 | AOT 安定性 | 正式(AOT)ビルド | E-01〜E-07 | クラッシュ・WER なし | [~] |

> 2026-07-26 の報告: 移動自体はディスク上で成功するが**表示が更新されない**（移動元にも移動先にも反映されない）
> → BUG-008 として起票し、表示中フォルダの監視による自動再読込を実装（2026-07-25）。再検証時は
> 「ドロップ後 1 秒以内に両ペインの一覧が更新されること」を E-01〜E-06 の合否条件に含める。

> 検証: 2026-07-20。合成マウス（SetCursorPos+mouse_event）の連続ドラッグを WinUI の D&D が受け付けず**自動化不可**。ドラッグ試行中もクラッシュなし（WER 0）。E-01〜E-08 は実機目視での確認が必要（pane-split E-05 と同じ制約）。

## ステータス凡例

- `[ ]` 未実施
- `[~]` 実施中
- `[x]` 合格
- `[!]` 不合格

### 追加分（2026-08-09: フォルダの行へのドロップ）

| ID | テスト名 | 前提条件 | 手順 | 期待結果 | ステータス |
|----|---------|---------|------|---------|-----------|
| E-10 | フォルダの行に落とす | 一覧にフォルダとファイルがある | ファイルをフォルダの行の上までドラッグして離す | **そのフォルダの中へ**移動する（表示中フォルダには残らない） | [ ] |
| E-11 | 落ちる先の強調 | E-10 の途中 | フォルダの行の上でドラッグを止める | その行が薄い青（`#CFE8FA`）に塗られ、選択色とは違って見える。行から外すと消える | [ ] |
| E-12 | 行以外に落とす | 一覧の余白／ファイルの行 | そこへ落とす | 従来どおり**表示中フォルダ**へ移動する | [ ] |
| E-13 | 自分自身の上 | フォルダを 1 つドラッグ | そのフォルダ自身の行の上で離す | 何も起きない（自分の中へは入らない）。エラーにならない | [ ] |
| E-14 | Ctrl 併用 | E-10 と同じ | Ctrl を押しながらフォルダの行へ落とす | そのフォルダの中へ**コピー**される |[ ] |
| E-15 | 強調の消え残り | E-10 のあと | ドロップ後の一覧を見る | 塗りが残っていない | [ ] |
