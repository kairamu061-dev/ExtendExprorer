# address-bar テストケース

> ## ⚠ この表の `[x]` は、**撤去した WinUI 3 版**のものです
>
> 2026-09-12 に UI 基盤を **素の Win32 ＋ Native AOT** へ載せ替え、旧実装を撤去しました。
> **ここの `[x]` / `[ ]` は旧実装での結果**で、現行実装の合否ではありません。
>
> **現行実装での状況は [e2e-coverage.md](../../win32-migration/e2e-coverage.md) を見てください。**
>
> **この違いは実際に不具合を隠しました。**`drag-drop` は旧実装では 16 件すべて
> `[ ]` / `[~]`（合成マウスを WinUI が受け付けず自動化できなかったため）ですが、
> 現行実装ではいちばん厚く確認した領域です。逆に `[x]` が並んでいても
> 現行実装では一度も触っていないものがありました——
> [BUG-034](../../../issues/tickets/BUG-034.md)（移植し忘れ 3 件）は、
> **この表を `[x]` のまま信じていたら見つかりませんでした。**


## ユニットテスト

テストプロジェクトは未整備（初期版は手動確認。親 dev-notes.md 参照）。

| ID | テスト名 | 入力 / 条件 | 期待結果 | ステータス |
|----|---------|------------|---------|-----------|
| U-01 | （未整備） | — | — | [ ] |

## インテグレーションテスト

| ID | テスト名 | 前提条件 | 手順 | 期待結果 | ステータス |
|----|---------|---------|------|---------|-----------|
| I-01 | （未整備） | — | — | — | [ ] |

## E2Eテスト（手動）

| ID | テスト名 | 前提条件 | 手順 | 期待結果 | ステータス |
|----|---------|---------|------|---------|-----------|
| E-01 | パンくず表示 | 任意フォルダ表示 | アドレスバーを見る | `[C:] › [Users] › …` のようにフォルダ単位で表示される | [ ] |
| E-02 | セグメント移動 | 深い階層を表示 | 途中のフォルダ名をクリック | そのフォルダへ移動し、パンくずが更新される（戻るで復帰可） | [ ] |
| E-03 | 表示追従 | 複数タブ/移動 | タブ切替・フォルダ移動・戻る/進む | 常にアクティブタブの現在パスを表示 | [ ] |
| E-04 | 編集モード | 任意フォルダ表示 | パンくずの余白（フォルダ名以外）をクリック | フルパスが全選択された編集ボックスになる | [ ] |
| E-05 | パス入力移動 | E-04 | 有効なパスを入力して Enter | そのフォルダへ移動し、パンくず表示に戻る | [ ] |
| E-06 | ファイルパス入力 | E-04 | ファイルのフルパスを入力して Enter | その親フォルダへ移動する | [ ] |
| E-07 | 無効パス | E-04 | 存在しないパスを入力して Enter | 「パスが見つかりません」が 3 秒表示され、移動しない（編集継続） | [ ] |
| E-08 | Esc 取消 | E-04 | Esc | 編集を破棄しパンくず表示に戻る（移動しない） | [ ] |
| E-09 | フォーカス喪失で復帰 | E-04 | Enter せず他所をクリック | 編集を破棄し元のパンくず表示に戻る | [ ] |
| E-10 | AOT 安定性 | 正式(AOT)ビルド | E-01〜E-09 | クラッシュ・WER なし | [ ] |

### 追加分（2026-07-27: セグメントのホバー表示）

| ID | テスト名 | 前提条件 | 手順 | 期待結果 | ステータス |
|----|---------|---------|------|---------|-----------|
| E-11 | マウスオーバーで強調 | 任意フォルダ表示 | パンくずの各フォルダ名にカーソルを重ねる | その部分だけ背景が薄い青（#CFE8FA）になり、クリックできることが分かる。押下でさらに濃くなる | [x] |
| E-12 | 余白は反応しない | E-11 | セグメントとセグメントの間（区切り「›」や余白）にカーソルを重ねる | 強調されない（クリックすると従来どおり編集モードに入る） | [x] |

## ステータス凡例

- `[ ]` 未実施
- `[~]` 実施中
- `[x]` 合格
- `[!]` 不合格
