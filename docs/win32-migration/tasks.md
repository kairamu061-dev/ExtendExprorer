# win32-migration タスク

## 進め方

**段階ごとに CI ビルド → 実機確認**という現行の運用をそのまま使う。
各段の終わりに**メモリを実測**して、目標 30MB からの乖離を早期に見つける
（最後にまとめて測ると、どの機能が重かったのか分からなくなる）。

現行 WinUI 3 版は**動く状態で残す**。ユーザーが日常的に使っているため、
新実装が同等になるまで置き換えない。

## 実装タスク一覧

<!-- ステータス: [ ] 未着手 / [~] 進行中 / [x] 完了 -->

### 第 0 段: 土台

- [x] スパイクで基盤を決定（`spikes/Win32Aot`・実測 25.7MB / コミット 6.8MB）
- [x] `src/ExtendExprorer.Win32/` を作成し、CI に追加（旧版と並走。成果物 `ExtendExprorer-win32-x64`・**4.6MB**）
- [x] ウィンドウ・メッセージループ・レイアウト（`WM_SIZE` で `[ツリー][スプリッタ][ペイン領域]` を計算）
- [x] マニフェスト（**comctl32 v6**・PerMonitorV2 DPI・長いパス）
- [x] 既存資産の移植: `Entry` / `ListResult` / `NaturalStringComparer` / session（モデル＋サービス）/ `FileSystemService`
- [x] シェル相互運用の土台（`StrCmpLogicalW`・`SHGetFileInfoW`。**アイコンはシステムイメージリストの番号だけを持つ**）
- [x] **メモリ実測 #0**（雛形の床）— **WS 14〜16MB / コミット 4.1MB**（2026-08-14 実測）。
      スパイク（25.7 / 6.8MB）を下回ったため、**csproj の絞り込み（`InvariantGlobalization` 等）は入れない**

### 第 1 段: 一覧（file-list）

- [x] `SysListView32`（`LVS_REPORT | LVS_OWNERDATA`）＋ システムイメージリスト
      （アイコン番号は**拡張子単位でキャッシュ**。実行ファイル等だけ実パスで引く）
- [x] 列（名前・更新日時・種類・サイズ）とヘッダクリックでのソート（自然順）＋ソート矢印
- [x] 移動（ダブルクリック・Alt+←/→/↑・マウスの戻る/進むボタン）と履歴
- [x] `FileSystemWatcher` による差分更新（追加＝末尾／リネーム＝位置固定／削除＝その行）
- [x] 隠し・システム属性の行を薄色に（`NM_CUSTOMDRAW`）／エラー・空フォルダの中央表示（`LVN_GETEMPTYMARKUP`）
- [x] 土台: `UiDispatcher`（ワーカー→UI スレッド）と `WndProc` の例外境界＋`error.log`
- [ ] **メモリ実測 #1**（ホーム / System32）← 実機確認待ち
- [ ] Backspace の割り当て（「戻る」か「上へ」か実機で確認してから合わせる）

### 第 2 段: タブとペイン

- [ ] タブ帯（自前描画・**折り返し**・幅は名前に合わせる・アクティブを上に広げる）
- [ ] タブの追加・切替・クローズ（中クリック／右クリックメニュー）
- [ ] ペイン分割とスプリッタ（レイアウト木は現行から移植）
- [ ] **非アクティブタブは一覧データを持たない**（BUG-016 の構造的対処）
- [ ] **メモリ実測 #2**（タブ 30 枚・ペイン 4 分割）← ここが目標達成の山場

### 第 3 段: ツリーとアドレスバー

- [ ] フォルダツリー（`SysTreeView32`・遅延展開・システムイメージリスト）
- [ ] ツリー幅のドラッグ調整
- [ ] パンくずアドレスバー（クリックで移動・ホバー強調）

### 第 4 段: 操作

- [ ] インライン リネーム（2 回目クリック・拡張子を除く選択・Tab で次の行）
- [ ] コンテキストメニュー（シェルの `IContextMenu`）
- [ ] ドラッグ＆ドロップ（フォルダの行へのドロップを含む）
- [ ] ショートカット（Ctrl+C/X/V・Delete 等）

### 第 5 段: 仕上げ

- [ ] session 復元（現行の `session.json` をそのまま読めること）
- [ ] `docs/explorer/*/test-cases.md` の E2E を新実装で通す
- [ ] **メモリ実測 #3**（完成形・目標 30MB 前後）
- [ ] 旧 WinUI 3 版の撤去・CI とドキュメントの一本化

## 依存関係

- 第 1 段 → 第 2 段（一覧が無いとタブに中身が無い）
- 第 2 段の「非アクティブタブはデータを持たない」→ メモリ実測 #2 の結果で方式を確定
- 第 4 段のシェル連携は現行から移植するだけなので、第 1〜3 段と並行して進められる

## 中断・撤回の条件

**メモリ実測 #2（タブ 30 枚）で目標から大きく外れたら、いったん止めて方式を見直す。**
第 5 段まで進んでから「届きませんでした」となるのが最悪なので、山場で必ず測る。
