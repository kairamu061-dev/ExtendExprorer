# file-list テストケース

> ## ⚠ この表の `[x]` は、**撤去した WinUI 3 版**のものです
>
> 2026-09-12 に UI 基盤を **素の Win32 ＋ Native AOT** へ載せ替え、旧実装を撤去しました。
> **ここの `[x]` / `[ ]` は旧実装での結果**で、現行実装の合否ではありません。
>
> **現行実装での状況は [e2e-coverage.md](../../win32-migration/e2e-coverage.md) を見てください。**
>
> **この違いは実際に不具合を隠しました。**`drag-drop` は旧実装では 16 件すべて
> `[ ]` / `[~]`（合成マウスを WinUI が受け付けず自動化できなかったため）ですが、
> 現行実装ではいちばん厚く確認した領域です。逆に `[x]` が並んでいても
> 現行実装では一度も触っていないものがありました——
> [BUG-034](../../../issues/tickets/BUG-034.md)（移植し忘れ 3 件）は、
> **この表を `[x]` のまま信じていたら見つかりませんでした。**


## ユニットテスト

テストプロジェクトは未整備（初期版は手動確認。導入は今後の課題 → 親 dev-notes.md）。

| ID | テスト名 | 入力 / 条件 | 期待結果 | ステータス |
|----|---------|------------|---------|-----------|
| U-01 | （未整備） | — | — | [ ] |

## インテグレーションテスト

| ID | テスト名 | 前提条件 | 手順 | 期待結果 | ステータス |
|----|---------|---------|------|---------|-----------|
| I-01 | （未整備） | — | — | — | [ ] |

## E2Eテスト（手動）

| ID | テスト名 | 前提条件 | 手順 | 期待結果 | ステータス |
|----|---------|---------|------|---------|-----------|
| E-01 | 起動時ホーム表示 | Windows 11 で exe 起動 | 起動する | ユーザプロファイル直下の一覧が表示され、ツールバーにパスが出る | [x] |
| E-02 | フォルダ移動 | E-01 | 任意のフォルダ行をダブルクリック | そのフォルダの内容に切り替わり、パス表示が更新される | [x] |
| E-03 | 上へ | E-02 | 「上へ」ボタンを押す | 親フォルダに戻る。ドライブルートでは「上へ」が無効化される | [x] |
| E-04 | 戻る / 進む | E-02 | 「戻る」→「進む」を押す | 履歴どおりに移動し、端ではボタンが無効化される | [x] |
| E-05 | ソート | E-01 | 「名前」ヘッダを 2 回クリック、次に「サイズ」をクリック | 昇順→降順に切替わる。列を変えると昇順から。常にフォルダが先頭グループ | [x] |
| E-06 | アクセス拒否 | E-01 | `C:\System Volume Information` 等へ移動を試みる | 一覧領域に「アクセスが拒否されました」が表示され、アプリは落ちず「戻る」で復帰できる | [x] |
| E-07 | 表示書式 | E-01 | 一覧の各列を目視確認 | 日時 `YYYY/MM/DD HH:mm`、フォルダのサイズは「—」、ファイルは KB/MB/GB 表記、種類は拡張子大文字（なしは「ファイル」） | [x] |
| E-08 | ソートのリセット | E-05 | サイズ降順にした状態で別フォルダへ移動する | 移動先では名前昇順（既定）に戻っている | [x] |
| E-09 | 隠し/システムの薄色表示 | E-01 | ホームや `C:\` で `NTUSER.DAT` / `$Recycle.Bin` / `pagefile.sys` 等を目視 | 隠し・システム属性の行だけ薄色（減光）で表示される | [x] |
| E-10 | UIA 行名 | E-01 | UIA インスペクタ等で一覧行の Name を確認 | 行の Name がファイル名になっている（クラス名ではない） | [x] |

E-01〜E-07 は 2026-07-05 の動作確認（run 28728613563, commit `2da7ec4`）、E-08〜E-10 は同日の再確認（run 28734759376, commit `5077431`）で合格。

### 追加分（2026-07-13: ファイルを開く）

| ID | テスト名 | 前提条件 | 手順 | 期待結果 | ステータス |
|----|---------|---------|------|---------|-----------|
| E-20 | ファイルを開く | 任意フォルダ表示中 | .txt 等をダブルクリック | 拡張子の既定アプリで開く | [x] |
| E-21 | 関連付けなし | 同上 | 関連付けのない拡張子をダブルクリック | シェルの「このファイルを開く方法」ダイアログが出る（クラッシュしない） | [x] |

> E-20/E-21 検証: 2026-07-19（commit `e63cef4`）で E-20 は不合格（既定アプリがあるのに「開く方法」チューザー） → BUG-004 起票。2026-07-20（commit `9f31c9e`）の再検証で E-20 合格（既定アプリで直接起動）。E-21 は当初から正常。フォルダダブルクリック移動の退行なし。

### 追加分（2026-07-19: リネーム・複数選択）

| ID | テスト名 | 前提条件 | 手順 | 期待結果 | ステータス |
|----|---------|---------|------|---------|-----------|
| E-30 | リネーム開始 | ファイルを 1 つ選択 | 間を空けてもう一度名前をクリック | 名前が TextBox になり、拡張子を除く部分が選択されている | [x] |
| E-31 | リネーム確定 | E-30 | 新しい名前を入力して Enter | ファイル名が変わり一覧に反映される（ソート維持） | [x] |
| E-32 | リネーム取消 | E-30 | Esc（または変更せずフォーカス移動） | 元の名前のまま | [x] |
| E-33 | ダブルクリックと区別 | ファイル選択済み | 素早く 2 回クリック | リネームにならず既定アプリで開く | [x] |
| E-34 | 同名衝突・不正文字 | E-30 | 既存名や `?` 入りの名前で Enter | シェルのエラーダイアログ（クラッシュしない） | [x] |
| E-35 | 編集中のショートカット | E-30 の編集中 | Ctrl+C/V・Delete | 文字列編集として動作（ファイル操作にならない） | [x] |
| E-36 | 複数選択時は開始しない | 複数選択 | 選択項目をクリック | リネームは開始されない | [x] |

> リネーム/複数選択の検証: 2026-07-20（`動作確認フィードバック_BUG-004-005と複数選択・ショートカット・配色・リネーム_2026-07-20.md` / commit `9f31c9e` / run `29672686209`）。
> E-20 は BUG-004 修正で解消（既定アプリで直接起動）。E-30/E-31/E-33/E-36 合格。E-32/E-34/E-35 は自動化制約で未確認（実機目視持ち越し）。

> 2026-07-26 の再検証（commit `e58c75c` / run `29745106204`）で **E-30〜E-36 すべて合格**
> （遅いダブルクリックは 10/10 でリネームに突入、編集ボックス幅も名前にフィット）。
> ただし「移動を挟むとリネームが開始しなくなる」退行を新たに検出 → BUG-007 として起票・修正。

### 追加分（2026-07-25: リネームの退行修正・自動更新・見た目）

| ID | テスト名 | 前提条件 | 手順 | 期待結果 | ステータス |
|----|---------|---------|------|---------|-----------|
| E-40 | 移動を挟んでもリネームできる | 任意フォルダ表示 | 一度リネーム編集に入る → 別フォルダへ移動 → 戻る → 遅い2回目クリック | リネーム編集に入る（BUG-007。Up・戻る・進む・パンくずのいずれの移動でも同じ） | [x] |
| E-41 | 外側クリックで確定 | E-30 の編集中 | 名前を変えて、一覧の別の行または空白をクリック | その場でリネームが確定し、一覧に反映される | [x] |
| E-42 | 編集ボックスの見た目 | E-30 | 編集ボックスを目視 | 行の高さに収まる 1px 枠の細いボックス（角丸・太い下線なし）。サンプルツールと同等 | [x] |
| E-43 | 選択色 | 任意フォルダ表示 | 1 件／複数件を選択 | 選択行が濃いめの青（#A6D3F3 系）で塗られ、非選択との差がはっきり分かる | [x] |
| E-44 | D&D 移動が表示に反映 | 2 ペインで別フォルダを表示 | 片方から他方へ項目をドラッグ＆ドロップ | 移動元から消え、移動先に現れる（1 秒以内。BUG-008） | [ ] |
| E-45 | 貼り付け・削除の自動反映 | 任意フォルダ表示 | Ctrl+V で貼り付け、Delete で削除 | 一覧が自動で更新される（手動の再移動が不要） | [x] |
| E-46 | 自動更新で選択が外れない | 複数選択した状態 | 別アプリでそのフォルダにファイルを 1 つ作る | 一覧に新ファイルが現れるが、選択していた項目の選択は維持される | [x] |
| E-47 | ショートカットのツールチップが出ない | 一覧表示 | 一覧の上にカーソルを数秒置く | 「Ctrl+C」等のツールチップが出ない（BUG-009）。Ctrl+C/X/V・Delete は従来どおり動く | [x] |
| E-48 | 監視できないパス | ネットワーク／権限のないフォルダ | そのフォルダを表示する | 自動更新が効かないだけで、クラッシュもエラー表示も起きない | [x] |

> E-40〜E-48 は 2026-07-27 の実機検証（commit `20c6501` / run `30166851462`）で**すべて合格**。
> 反映速度は貼り付け 0.51 秒・削除 0.25 秒・外部変更 0.56 秒。選択維持も確認。
> **E-44（D&D そのもの）だけは合成ドラッグを WinUI が受け付けず自動検証不可**で、実機目視の持ち越し。

### 追加分（2026-07-27: リネームのフォーカス・文字サイズ）

| ID | テスト名 | 前提条件 | 手順 | 期待結果 | ステータス |
|----|---------|---------|------|---------|-----------|
| E-49 | フォーカスせずに別ファイルを選択 | リネーム編集ボックスを開いた直後 | 編集ボックスを**一度もクリックせずに**別のファイル行をクリック | リネームが確定して編集ボックスが閉じ、**クリックした行が選択される** | [~] |
| E-50 | 拡張子を除く部分が既定で選択 | 何度か移動・再読込した後の行 | 遅い2回クリックでリネームを開始 | 毎回、拡張子を除いたファイル名部分が選択された状態で始まる（フォルダは全選択） | [x] |
| E-51 | 文字サイズが一覧と同じ | E-50 | 編集ボックスの文字と隣の行のファイル名を見比べる | 同じ大きさに見える（編集開始で文字が小さくならない） | [x] |

> E-49〜E-51 は 2026-07-30 の実機検証（commit `566d953` / run `30281477921`）で E-50（10/10）・E-51 合格。
> **E-49 は「確定・クローズは正常だが、クリックした行が選択されない」で不合格** → BUG-012 として起票・修正（再検証待ち）。

### 追加分（2026-07-30: アイコンの劣化・自然順ソート）

| ID | テスト名 | 前提条件 | 手順 | 期待結果 | ステータス |
|----|---------|---------|------|---------|-----------|
| E-52 | 一覧アイコンが劣化しない | 起動直後（黄色いフォルダアイコン） | リネーム・貼り付け・削除・タブ操作・自動更新を含む操作を長時間続け、ときどき一覧のアイコンを目視 | 黄色いシェルアイコンのまま。輪郭だけのグリフに変わらない（BUG-010） | [ ] |
| E-52b | **コールドスタート直後**のアイコン | アプリを終了した状態から起動 | **起動して最初に表示された一覧**のアイコンを、移動する前に目視（5 回程度繰り返す） | 初回表示から色付きのシェルアイコン。輪郭グリフにならない（BUG-015）。**移動してから見ないこと** | [x] |
| E-52c | コールドスタート（ホーム以外） | 最後にホーム以外（`C:\Windows` 等）を開いた状態で終了 | 起動して初回の一覧アイコンを目視 | E-52b と同じ。フォルダに依存しない | [x] |
| E-53 | 劣化しても自己回復する | E-52 でもし劣化した場合 | 別のフォルダへ移動する／同じフォルダを再読込する | 次の読み込みでシェルアイコンに戻る（再起動を要しない） | [ ] |
| E-54 | 名前の自然順ソート | `File1.txt` `File2.txt` `File10.txt` `File20.txt` を作る | 名前列で昇順に並べる | `File1 → File2 → File10 → File20` の順（`File10` が `File2` より前に来ない）。エクスプローラーと同じ並び | [ ] |
| E-55 | ツリーも自然順 | 数字付きフォルダ（`sub1` `sub2` `sub10`）を作る | 左ツリーで親を展開する | 一覧と同じ自然順で並ぶ | [ ] |
| E-56 | 書き込み中の再読込 | 大きなファイル（数百MB）を一覧表示中のフォルダへコピー | コピー中の一覧を観察 | 一覧が数百ミリ秒ごとにちらつき続けない。コピー完了後にサイズが反映される。**コピー開始直後にファイル自体は現れる** | [ ] |

> E-49 / E-52〜E-56 は commit `3ad1947` / run `30543221490`（aot・jit 両 success・警告なし）で実機検証依頼中
> （`tmp/確認/確認依頼_アイコン劣化・タブアイコン・自然順_2026-07-30.md`）。

### 追加分（2026-08-04: 差分更新・Tab リネーム・サイズ感）

| ID | テスト名 | 前提条件 | 手順 | 期待結果 | ステータス |
|----|---------|---------|------|---------|-----------|
| E-62 | 追加は末尾 | 一覧表示中 | 別アプリでそのフォルダにファイルを作る | 一覧の末尾に追加されるだけ。他の行は動かない | [ ] |
| E-63 | リネームは位置固定 | 一覧表示中 | ファイルをリネームして Enter | 同じ位置のまま名前だけ変わる（並べ替えが走らない） | [ ] |
| E-64 | 削除はその行だけ | 一覧表示中 | 別アプリでファイルを削除 | その行だけ消える | [ ] |
| E-65 | ホームで勝手に更新されない | ホームを表示 | 3 分放置する | 一覧が勝手に更新されない | [ ] |
| E-66 | 貼り付け・削除 | 一覧表示中 | Ctrl+V / Delete | 追加は末尾・削除はその行だけ。全体が再構築されない | [ ] |
| E-67 | 明示的な並べ替え | E-62 の後 | 列ヘッダをクリック | このときは並べ替わる | [ ] |
| E-68 | 移動で並び直る | E-62 の後 | 別フォルダへ移動して戻る | 名前順に並んでいる | [ ] |
| E-69 | Tab で次のリネーム | リネーム編集中 | Tab を押す | 確定して 1 つ下の項目のリネームが始まる（拡張子を除く部分が選択済み） | [ ] |
| E-70 | Shift+Tab で前のリネーム | リネーム編集中 | Shift+Tab を押す | 確定して 1 つ上の項目のリネームが始まる | [ ] |
| E-71 | 端での Tab | 一番下の項目のリネーム中 | Tab を押す | 確定するだけで進まない（エラーにならない） | [ ] |
| E-72 | 行の高さ | 起動直後 | 一覧とツリーの行ピッチを実測 | どちらも 19px 前後で揃っている | [ ] |
| E-73 | 見切れ | 日本語名のファイル・フォルダ | 名前の下端を拡大して目視 | 切れていない（行を詰めたため要確認） | [ ] |

### 追加分（2026-09-13: 「PC」の一覧）

> E-80〜E-98 は 2026-09-15（`1e83ede`）と 2026-09-17（`6f98e5b`）の実機確認で合格。
> **E-89 だけは、準備できていないドライブのある環境が無く未確認。**
> E-92b（改名が始まらない）は [BUG-035](../../../issues/tickets/BUG-035.md) の修正確認。


| ID | テスト名 | 前提条件 | 手順 | 期待結果 | ステータス |
|----|---------|---------|------|---------|-----------|
| E-80 | ドライブの一覧 | 起動直後 | ツリーの「PC」をクリック | 一覧に**ドライブが並ぶ**。列が **名前 / 種類 / 合計サイズ / 空き領域** に入れ替わる | [x] |
| E-81 | 容量の数字 | E-80 | 合計サイズ・空き領域を、エクスプローラーの「PC」と見比べる | **同じ値**（GB 表記の丸めの差は可） | [x] |
| E-82 | ドライブの種類 | E-80 | 種類の列 | ローカル ディスク / リムーバブル ディスク / CD ドライブ 等 | [x] |
| E-83 | アイコン | E-80 | 各行のアイコン | ドライブごとの絵（システムドライブは Windows マーク付き） | [x] |
| E-84 | ドライブを開く | E-80 | 行をダブルクリック | **そのドライブの直下へ移動**。戻るで「PC」に戻れる | [x] |
| E-85 | Enter でも開く | E-80 | 行を選んで Enter | 同上 | [x] |
| E-86 | 「上へ」 | E-80 | 「上へ」ボタン | **無効**（親が無い） |[x] |
| E-87 | パンくず | E-80 | アドレスバー | **「PC」**と 1 区切りで出る（合言葉がそのまま見えない） | [x] |
| E-88 | タイトルバー | E-80 | ウィンドウのタイトル | `PC - ExtendExprorer` | [x] |
| E-89 | 準備できていないドライブ | 空の光学ドライブがある環境 | E-80 | **行は出る**。合計・空きが「—」 | [ ]（該当するドライブのある環境が無く未確認・2026-09-17） |
| E-90 | ★ **削除が効かない** | E-80 | ドライブの行を選んで `Delete` | **何も起きない。**確認ダイアログも出ない・落ちない | [x] |
| E-91 | ★ コピー・切り取り | E-80 | `Ctrl+C` / `Ctrl+X` | 何も起きない・落ちない | [x] |
| E-92 | 右クリック | E-80 | 行・背景とも右クリック | メニューが出ない・落ちない | [x] |
| E-92b | ★ **改名が始まらない** | E-80 | ドライブの行を選択 → 間を空けてもう一度クリック。**F2** と右クリックの「名前の変更」も | **編集欄が開かない**（`[rename] 「PC」なので始めない`）・落ちない | [x] |
| E-93 | 並べ替え | E-80 | 列ヘッダをクリック | **並びが変わらない**（ドライブ文字の順のまま）・落ちない | [x] |
| E-94 | ドロップ | E-80 | 別のペインからドライブの行へドラッグ | **落とせない**（受け取らない）・落ちない | [x] |
| E-95 | F5 | E-80 | USB を挿す／抜いて F5 | 一覧が読み直される | [x] |
| E-96 | ★ **session の往復** | 「PC」を開いたタブを含む状態で終了 | 起動する | **「PC」のタブが残っている**（`Directory.Exists` で落とされない） | [x] |
| E-97 | 普通のフォルダへ戻る | E-84 のあと戻る | 列と中身 | 列が **名前 / 更新日時 / 種類 / サイズ** に戻り、容量の値が残らない | [x] |
| E-98 | タブの切り替え | 「PC」のタブと普通のフォルダのタブを 1 枚ずつ | 交互に切り替える | そのつど列が入れ替わる。混ざらない | [x] |
### 追加分（2026-09-18: フォルダを先頭にまとめるか）

| ID | テスト名 | 前提条件 | 手順 | 期待結果 | ステータス |
|----|---------|---------|------|---------|-----------|
| E-100 | ダウンロードは混合 | — | ダウンロードフォルダを開いて名前順にする | **フォルダとファイルが混ざって並ぶ**（フォルダが先頭にまとまらない） | [x] |
| E-101 | 他は従来どおり | — | 普通のフォルダを開く | **フォルダが先頭にまとまる**（従来どおり） | [x] |
| E-102 | ★ 切り替え | E-101 | **列見出しを右クリック** | 「フォルダを先頭にまとめる」が出て、**チェックが入っている** | [x] |
| E-103 | 切り替えが効く | E-102 | その項目を選ぶ | **混合になる。**もう一度選ぶと戻る | [x] |
| E-104 | ダウンロードでのチェック | E-100 | 列見出しを右クリック | **チェックが外れている** | [x] |
| E-105 | ★ フォルダごとに覚える | E-103 のあと | 別のフォルダへ行って戻る | **変えた設定が残っている。**別のフォルダは影響を受けていない | [x] |
| E-106 | ★ session に残る | E-103 のあと終了 → 起動 | そのフォルダを開く | **変えた設定が残っている** |[x] |
| E-107 | 既定に戻すと消える | E-103 で変えたものを元に戻す → 終了 | `session.json` の `FolderSort` | **その項目が消えている**（既定と同じものは覚えない） | [x] |
| E-108 | 「PC」では出ない | ツリーの「PC」を開く | 列見出しを右クリック | **メニューが出ない**・落ちない | [x] |
| E-109 | 並べ替えとの併用 | E-103 | 更新日時・種類・サイズでも並べ替える | どの列でも混合のまま | [x] |
## ステータス凡例

- `[ ]` 未実施
- `[~]` 実施中
- `[x]` 合格
- `[!]` 不合格

> 2026-08-09 検証（AOT `47da9ba` / run `31272258402`）: E-52b = コールドスタート 14/14 で初回から色付き、E-52c = 4/4。修正前（`ee80106`）は約 85% が色なしだった。
